<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>
    <!--  <appender name="PACKAGE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
          <file>logs/specific-package.log</file>
          <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
              <fileNamePattern>logs/specific-package-%d{yyyy-MM-dd}.log</fileNamePattern>
              <maxHistory>30</maxHistory> &lt;!&ndash; Keep 30 days of logs &ndash;&gt;
          </rollingPolicy>
          <encoder>
              <pattern>%d{yyyy-MM-dd} [%thread] %-5level %logger{36} - %msg%n</pattern>
          </encoder>
      </appender>-->
    <appender name="PACKAGE_FILE_CONTROLLER" class="ch.qos.logback.core.FileAppender">
        <file>logs/controller.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Logger configuration for a specific package -->
    <logger name="com.sparta.mdesigns.controllers" level="TRACE" additivity="false">
        <appender-ref ref="PACKAGE_FILE_CONTROLLER" />
    </logger>

</configuration>